name: test

on:
  push:
    branches:
      - main
  pull_request:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  test:
    uses: livMatS/dtool-framework-workflows/.github/workflows/dtool-lookup-api-test.yml@main
